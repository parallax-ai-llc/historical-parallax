Translation complete. The file has been written to `content/articles/zh/gedeon-kyungu-mutanga.md` with:

- YAML frontmatter preserved exactly as-is
- All body text (headings, paragraphs, table cells, list items) translated to Simplified Chinese
- All Markdown formatting and URLs kept unchanged
- The Korean supplementary section (추가 정리) also translated to Chinese

Note: there was a pre-existing file `g-d-on-kyungu-mutanga.md` with a different `id` and slightly different content — the new file uses the exact `id` from the source article you provided.