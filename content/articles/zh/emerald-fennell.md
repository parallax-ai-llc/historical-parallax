The file `content/articles/emerald-fennell.md` doesn't contain an actual Markdown article — it only contains the literal text:

```
Article written to `content/articles/emerald-fennell.md`.
```

There's no article content to translate. Could you provide the actual Markdown content for the Emerald Fennell article, or point me to where it exists?