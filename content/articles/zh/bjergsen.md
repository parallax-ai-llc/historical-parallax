The file `content/articles/bjergsen.md` exists but is empty (0 bytes). It appears the article content was never actually written to it.

Could you provide the article content you'd like translated? You can either:
1. Paste the Markdown directly here
2. Confirm the article exists elsewhere so I can find it