The file at `content/articles/gaggan-anand.md` doesn't contain an actual Markdown article — it only contains the description text you provided. There's no YAML frontmatter, headings, or body content to translate.

Could you provide the actual article content, or point me to where the full Markdown file is located?