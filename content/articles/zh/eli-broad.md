The file only contains that single line — there's no actual article content to translate. Could you provide the full Markdown article for Eli Broad? The file at `content/articles/eli-broad.md` currently just contains the placeholder text:

```
Article written to `content/articles/eli-broad.md`.
```

I need the actual article content (with YAML frontmatter, headings, paragraphs, etc.) to translate it into Simplified Chinese.