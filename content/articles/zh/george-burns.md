The file `content/articles/george-burns.md` only contains the single line:

```
Article written to `content/articles/george-burns.md`.
```

This is a placeholder/confirmation message, not an actual article. There's no YAML frontmatter, headings, or body text to translate.

Could you provide the actual George Burns article content? Or if it was generated in a previous conversation, it may not have been saved correctly — the file just has a status message instead of the article.