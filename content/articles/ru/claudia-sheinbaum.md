The file at `content/articles/claudia-sheinbaum.md` only contains a 420-byte summary/description — not an actual Markdown article with YAML frontmatter and body content. There's no real article to translate.

Could you either:
1. **Provide the actual article content** (with YAML frontmatter, headings, paragraphs, etc.) that you'd like translated to Russian, or
2. **Point me to the correct file** if the article lives elsewhere?

Without the source article text, I can't produce a translation.