The file `content/articles/bjergsen.md` only contains the description of what the article should cover — the actual article content was never written to it. There's nothing to translate yet.

You'll need to write the article first (with proper YAML frontmatter and body text), then I can translate it into Russian.