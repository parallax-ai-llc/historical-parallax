The file truly only contains that placeholder text. There is no actual article content to translate — `content/articles/david-guetta.md` just has the single line:

```
Article written to `content/articles/david-guetta.md`.
```

Could you provide the actual Markdown article for David Guetta? I need the full content (YAML frontmatter + body text) to translate it into Russian.