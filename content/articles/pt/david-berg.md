There's only that one file, and it contains just the placeholder text `Article written to content/articles/david-berg.md.` — not an actual Markdown article with frontmatter and body content.

Could you provide the actual article content for `david-berg.md`? The file currently only contains a single placeholder line, so there's nothing to translate.