The file exists but only contains that placeholder text. There's no actual article content to translate. Could you provide the actual Markdown content for the Ctesibius article? The file at `content/articles/ctesibius.md` currently only contains a single line:

```
The article for Ctesibius has been written to `content/articles/ctesibius.md`.
```

It doesn't have any YAML frontmatter or article body to translate.