The file doesn't contain an actual article — it only has a description/placeholder text. There's no Markdown article with YAML frontmatter to translate.

Could you provide the actual article content for `anitta.md`? Either paste it here or write it to the file first, and then I can translate it into Brazilian Portuguese.