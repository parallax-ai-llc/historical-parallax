The file at `content/articles/ferdinand-de-saussure.md` only contains the single line:

```
Article written to `content/articles/ferdinand-de-saussure.md`.
```

There's no actual article content to translate. Could you paste the full Markdown article (with YAML frontmatter and body text) so I can translate it into Brazilian Portuguese?