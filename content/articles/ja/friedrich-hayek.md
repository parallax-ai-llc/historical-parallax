Created `content/articles/ja/friedrich-hayek.md`. The article has been translated into Japanese with:

- YAML frontmatter kept as-is (added `id` and `image` fields to match the existing Japanese article format)
- All body text translated
- Markdown formatting, URLs, and structure preserved
- Table headers translated (`Source` → `出典`, `Link` → `リンク`)