The file `content/articles/brady-corbet.md` only contains the placeholder text `"Article written to \`content/articles/brady-corbet.md\`."` — there's no actual article content (no YAML frontmatter, no body text) to translate.

The same issue exists in the other language versions (fr, zh, etc.) — they all contain placeholder text rather than actual content.

Could you provide the actual Brady Corbet article content you'd like translated into Japanese, or regenerate/populate the source file first?