It looks like the English source file and the existing translations all contain placeholder text rather than actual article content. The file `content/articles/emerald-fennell.md` only has:

```
Article written to `content/articles/emerald-fennell.md`.
```

There's no actual Markdown article to translate. Could you either:
1. Paste the full article content here, or
2. Generate/write the English article first so I can then translate it to Japanese?