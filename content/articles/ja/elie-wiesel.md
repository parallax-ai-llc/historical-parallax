The file has always only contained that placeholder text. It was never actually generated with real content. There is no article to translate — the file at `content/articles/elie-wiesel.md` only contains the line:

```
Article written to `content/articles/elie-wiesel.md`.
```

This appears to be a logging/status message that was accidentally written as the file content instead of the actual article. I need the real article content to translate. Could you provide the actual Markdown article for Elie Wiesel, or would you like me to check if there's a script or data source that can generate it?