The source file at `content/articles/fred-hollows.md` only contains a placeholder — not an actual article. There's no real content to translate.

Could you provide the actual Markdown article content for Fred Hollows? It appears the file was never populated with a real article — it just contains the line:

```
Article written to `content/articles/fred-hollows.md`.
```

If the article was generated in a previous conversation, it may need to be regenerated first before I can translate it into Japanese.