The file `content/articles/bjergsen.md` doesn't contain an actual article — it only holds the description message (285 bytes, 0 newlines). The article content was never written to it.

Could you paste the article text you'd like translated, or write the article to the file first?