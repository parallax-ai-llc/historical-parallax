The file `ferran-adria.md` only contains the description text, not an actual article. There's no real Markdown article with YAML frontmatter to translate.

Could you provide the actual article content for Ferran Adrià? The file currently only contains the summary paragraph you shared, not a proper Markdown article with YAML frontmatter and body text.