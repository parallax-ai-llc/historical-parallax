The file `content/articles/edgar-wright.md` doesn't contain an actual article — it only has the placeholder text:

```
Article written to `content/articles/edgar-wright.md`.
```

It looks like the article generation step didn't write real content to this file. Would you like me to:

1. **Generate the Edgar Wright article first** (using the same format as other articles like `damien-chazelle.md`), and then translate it to Arabic?
2. Or do you have the actual article content elsewhere that you can paste?