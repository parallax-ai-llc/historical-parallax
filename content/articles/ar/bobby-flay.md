The file `content/articles/bobby-flay.md` only contains the placeholder text `Article written to content/articles/bobby-flay.md.` — it doesn't have actual article content.

Could you paste the actual article content for Bobby Flay that you'd like translated into Arabic? Or if there's a script that generates it, we can run that first.