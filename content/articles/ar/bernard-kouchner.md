The file `content/articles/bernard-kouchner.md` only contains the literal text:

> The article for Bernard Kouchner has been written to `content/articles/bernard-kouchner.md`.

There's no actual article content to translate. It looks like the file was created with just a placeholder message instead of real article text.

Could you:
1. Paste the article content here directly, or
2. Check how the article was supposed to be generated and re-run it

I can then translate it into Arabic right away.