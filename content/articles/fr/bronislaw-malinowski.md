The article file at `content/articles/bronislaw-malinowski.md` only contains the stub message — the actual content was never written. The file is empty of real article data (just 100 bytes containing the creation notice).

Could you share the actual article content you'd like translated? Or if there's a script that was supposed to generate it, I can help run that first.