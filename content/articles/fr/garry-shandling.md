The file `content/articles/garry-shandling.md` only contains the placeholder text:

> The article for Garry Shandling has been written to `content/articles/garry-shandling.md`.

It's 90 bytes — there's no actual article content (no YAML frontmatter, no body text). It looks like the article was never actually written to the file, or the write failed silently.

Could you provide the actual Markdown article content for Garry Shandling so I can translate it into French?