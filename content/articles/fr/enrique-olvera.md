The file `content/articles/enrique-olvera.md` doesn't contain an actual article — it only has the single line:

```
The article for Enrique Olvera has been written to `content/articles/enrique-olvera.md`.
```

There's no real Markdown article with YAML frontmatter to translate. Could you provide the actual article content, or did the article generation perhaps fail in a previous step?