The file `content/articles/evan-mobley.md` is essentially empty (0 lines) — it only contains a single-line description, not an actual article with YAML frontmatter and body text.

There's no article content to translate. To proceed, I'd need either:

1. The **actual article content** (with YAML frontmatter, headings, paragraphs, etc.) pasted or provided
2. Or to **generate the article first**, then translate it

Could you provide the full article text?