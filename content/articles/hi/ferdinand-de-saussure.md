The file `content/articles/ferdinand-de-saussure.md` only contains the placeholder line `Article written to ...` — there's no actual article content (no YAML frontmatter, no body text) to translate.

It looks like the article was never properly written to the file. Would you like me to:

1. **Generate** the Ferdinand de Saussure article first (following the project's article format), then translate it to Hindi?
2. Or do you have the article content to paste here?